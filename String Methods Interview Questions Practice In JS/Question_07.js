// Reverse a String: Write a function that takes a string as input and returns the string reversed.

// let str = " Hello World ";

// let functionReverse = (string) => {
//   reversed_Str = "";
//   for (let i = string.length - 1; i >= 0; i--) {
//     reversed_Str += string[i];
//   }
//   return reversed_Str;
// };

// console.log(functionReverse(str));

// Check either the string is palindrome or not

// let str = "madam";

// let functionReverse = (string) => {
//   reversed_Str = "";
//   for (let i = string.length - 1; i >= 0; i--) {
//     reversed_Str += string[i];
//   }

//   if (string == reversed_Str) {
//     return `The string ${string} is palindrome`;
//   } else {
//     return `The string ${string} is not  palindrome`;
//   }
// };

// console.log(functionReverse(str));

// Improved code:

// reversing a string

// let str = "Hello";

// function reverseStr(string) {
//   return string.split("").reverse().join("");
// }

// console.log(reverseStr(str));

// check either it is palindrome or not:

// let str = "maam";

// function reverseStr(string) {
//   let reverseString = string.split("").reverse().join("");
//   return reverseString == str
//     ? `The string ${string} is palindrome`
//     : `The string ${string} is not palindrome`;
// }

// console.log(reverseStr(str));
